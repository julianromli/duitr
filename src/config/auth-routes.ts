/** Where unauthenticated users are sent when accessing protected routes */
export const UNAUTHENTICATED_REDIRECT = '/landing' as const;

/** Production canonical site URL (with www for SEO consistency) */
export const CANONICAL_SITE_URL = 'https://www.duitr.my.id' as const;
